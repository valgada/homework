function reading(totalPages, pagesPerHour, days) {

    let totalTime = totalPages / pagesPerHour;
    let requiredHours = totalTime / days;

    console.log(requiredHours);

}
reading(212,20 ,2);
reading(432,15 ,4);
